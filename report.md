# 定日镜场优化算法说明

本文档只说明当前程序的算法实现，不代替竞赛论文，也不宣称对非凸布局问题得到数学意义上的全局最优。

## 1. 当前实现解决什么问题

程序读取 `problem/附件.xlsx` 中的 1745 个定日镜坐标，并按题目规定的 12 个日期、每个日期 5 个当地时间计算年均指标。坐标系为东、北、天顶，长度单位为米，功率单位为 kW。

当前版本已经删除原程序中固定的阴影效率 `0.95` 和截断效率 `0.90`。这两个效率现在由几何光线追踪得到，随太阳位置、镜面布局、镜面尺寸、安装高度和集热器几何尺寸变化。

## 2. 太阳与镜面模型

太阳方向直接计算为东、北、天顶三个分量：

```text
s = (-cos(delta) sin(omega),
     sin(delta) cos(phi) - cos(delta) sin(phi) cos(omega),
     sin(delta) sin(phi) + cos(delta) cos(phi) cos(omega))
```

这样可以避免由方位角反三角函数带来的上午、下午分支错误。DNI 和大气透射率使用题目附录公式；镜面中心到集热器中心的距离超过 1000 m 时程序会报错，而不是继续套用公式。

设镜心为 `c`，集热器中心为 `T`，则：

```text
t = (T-c) / |T-c|
n = (s+t) / |s+t|
```

其中 `n` 为平面镜法向。镜面宽度方向取水平向量 `u`，镜面高度方向为 `v = n × u`，所以镜面的上下边始终平行地面。镜面上任一点写作：

```text
p = c + xi * width * u + zeta * height * v
```

其中 `xi`、`zeta` 属于 `[-1/2, 1/2]`。

太阳并非单一射线。程序默认使用视半径 4.65 mrad 的均匀亮度太阳圆盘，并用扰乱 Sobol 点同时采样镜面面积和太阳圆盘方向。对太阳方向 `s'`，反射方向为：

```text
r = 2 * (n·s') * n - s'
```

基准模型为理想平面镜；镜面斜率误差只在敏感性分析中启用。

## 3. 阴影、遮挡与截断

对每一条采样光线，程序进行两次几何追踪：

1. 沿太阳到镜面的入射传播方向检查其他矩形镜、塔身和集热器。
2. 沿镜面反射方向检查其他矩形镜、塔身以及有限尺寸集热器。

矩形镜采用“射线与镜面平面求交，再检查宽度和高度范围”的精确判定。集热器采用有限圆柱模型，半径为 3.5 m，离地高度范围为 76–84 m；只把第一次命中圆柱侧面的光线计入接收，命中端面不计入外表受光面积。

对每面镜、每个时刻，令采样光线权重总和为 `C`，消除入射阴影和反射遮挡后的权重为 `S`，最终命中圆柱侧面的权重为 `H`，采样数为 `K`，则：

```text
余弦效率       = C / K
阴影后效率     = S / C
截断效率       = H / S
镜面功率       = DNI * 面积 * 大气透射率 * 反射率 * H / K
```

入射阴影和反射遮挡按布尔并集统计，同一条光线不会被重复扣损。截断效率和遮挡效率使用同一批光线计算，保留二者的相关性。异构镜场的场均效率按镜面面积加权，功率先在每个时刻对所有镜面求和，再对 60 个时刻等权平均。

## 4. 约束处理

程序显式检查：

- 镜面宽度、高度在 2–8 m 内，且宽度不小于高度；
- 安装高度在 2–6 m 内；
- 镜面绕水平轴全范围转动时最低点高于地面；
- 镜心位于半径 350 m 圆形区域内；
- 镜心距塔至少 100 m；
- 任意两面镜的底座中心距离至少为两者较大宽度加 5 m；
- 新设计中镜面外接圆完全位于场地内，并完全避开塔周禁区。

第一问保留附件给定坐标，只对其进行计算；第二、三问使用上述保守几何约束。

## 5. 第二问的优化策略

第二问要求所有镜面尺寸和安装高度相同，目标为：

```text
最大化：年平均输出功率 / 镜面总面积
约束：年平均输出功率 >= 60 MW
```

搜索变量包括塔址东、北坐标，统一镜面宽度和高度，统一安装高度，维护间距余量，径向或横向行距，布局旋转角、两个相位、布局类型和径向分带长度。

布局类型包括三角交错、分带径向交错和遮挡尺度自适应径向交错。生成布局后仍逐对检查镜间距，不依赖“理论上交错”这一假设。

搜索分为五个阶段：

1. 用 Sobol 参数种群和人工暖启动布局生成候选；
2. 先忽略镜间相互作用计算乐观上界，排除明显不可能达到 60 MW 的方案；
3. 对剩余方案进行完整 60 时刻的少光线交互追踪；
4. 对接近达标但容量不足的方案进行专门的容量恢复搜索，而不是直接丢弃；
5. 对多个不同参数区域进行高精度追踪，再用差分扰动、坐标搜索和删镜重算提高单位面积功率。

删镜时每次都重新计算遮挡。删除低收益镜面不会增加新的遮挡，因此在相同采样下可以安全地把多余功率换成更小的镜面面积。

## 6. 第三问的优化策略

第三问从第二问的可行解开始，而不是复制第二问结果。当前实现把镜场按径向距离和相对方位分成 6 组，对每组分别尝试：

- 减小或增大镜面宽度；
- 减小或增大镜面高度；
- 调整安装高度；
- 调整部分镜面位置；
- 交换低密度位置和高收益候选位置；
- 对塔址进行小范围平移；
- 对单面镜进行局部尺寸微调。

每次修改后重新进行完整交互追踪。只有同时满足“年均功率达到 60 MW”和“单位面积功率提高”两个条件，修改才会被接受。若尺寸变化导致容量不足，程序会从未使用且满足间距约束的候选位置补充镜面，再重新追踪。

## 7. Python 文件与运行方式

推荐只从根目录运行统一入口：

```bash
conda activate tf2
python solve_mirror_field.py --help
python solve_mirror_field.py all --profile research --workers 4
```

各阶段命令如下：

```bash
# 问题 1：附件镜场的高精度计算
python solve_mirror_field.py q1 --output output/validated --rays 4096

# 问题 2：统一镜型搜索
python solve_mirror_field.py q2 --output output/validated --profile research --workers 4

# 问题 3：异构镜型搜索，需要先完成问题 2
python solve_mirror_field.py q3 --output output/validated --profile research --workers 4

# 独立复核、收敛分析和敏感性分析
python solve_mirror_field.py verify --output output/validated --rays 4096 --replicates 4

# 流程调试，搜索量较小，不用于提交最终结果
python solve_mirror_field.py q2 --output output/smoke --profile smoke --workers 2
```

也可以直接调用模块，但这些文件不是独立的命令行程序：

```bash
python -c "from heliostat.io import load_attachment; print(len(load_attachment()))"
python -c "from heliostat.optics import evaluate; from heliostat.io import load_attachment; print(evaluate(load_attachment(), rays=128).annual_power_kw)"
python -m unittest discover -s tests -v
```

`heliostat/model.py` 提供 `Site`、`Field`、太阳和约束函数；`heliostat/geometry.py` 提供圆柱和矩形相交及邻域筛选；`heliostat/optics.py` 提供 Sobol 光线、NumPy 参考追踪和 C++ 加速追踪；`heliostat/optimize.py` 提供第二、三问搜索；`heliostat/verification.py` 提供独立复核；`heliostat/io.py` 负责读取附件和写出结果；`heliostat/plots.py` 生成布局、功率和收敛图。

## 8. 数值验证与输出

程序包含独立的 NumPy 参考追踪和 C++ 加速追踪。测试覆盖太阳方向、反射定律、圆柱相交、矩形边界、空间筛选、能量分解、排列不变性、删去遮挡物后功率不下降和设计约束。

最终验证使用多组独立扰乱 Sobol 样本，并输出单侧置信下界。只有独立复核后的下界仍不低于 60 MW，方案才被视为通过额定功率检查。另有光线数收敛、塔身半径、太阳视半径和镜面斜率误差敏感性分析。

默认输出在 `output/validated/`，包括逐镜设计参数、逐月和逐时刻 CSV、搜索记录、验证 JSON、收敛和敏感性分析，以及布局图。`problem/` 下的原始附件和模板始终只读。

## 9. 当前物理假设

题目没有给出塔身半径、太阳圆盘亮度分布和镜面制造误差，因此程序将以下量显式设为可修改参数：塔身半径 2 m、太阳视半径 4.65 mrad、均匀亮度太阳圆盘、理想平面镜和反射率 0.92。这些假设不应被解释为真实电站的测量值；若获得更准确的工程数据，只需要修改 `Site` 参数并重新运行验证。

当前优化属于有明确预算和记录的混合启发式搜索。它比原先的固定效率和单纯逐镜排序更完整，但没有全局最优性证明；增加搜索预算、扩展布局族或使用更强的局部搜索仍可能得到更好的方案。
