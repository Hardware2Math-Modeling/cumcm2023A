我已经把论文（`docs/paper.tex`）、全部源代码（`heliostat/*.py`、`raytrace.cpp`）、测试、README/report/algorithm 说明文档，以及题目原文和输出产物都读完了。下面是结论和拆解。

---

## 一、总体结论（先回答"算法有没有问题"）

**核心光学计算是正确的、且写得相当扎实**，但存在几处**数据/口径不一致**和**未给定的物理假设**，需要你注意。

**正确的部分（经我逐行核对，逻辑成立）：**
- 太阳位置（东-北-天顶三分量，规避方位角反三角分支）、赤纬、时角、DNI、大气透射率公式，与题目附录完全一致；
- 余弦效率、反射定律、镜面上下边水平、有限圆柱集热器（侧面/端面）首次相交判定；
- 能量账本 `η_cos = C/K`、`η_sb = S/C`、`η_trunc = H/S`，功率 `P = DNI·A·η_at·η_ref·(H/K)`，阴影/遮挡用布尔并集、同一条光线不重复扣损——这一套我推演过，数学上是自洽的（`H/K ≡ η_cos·η_sb·η_trunc`）；
- NumPy 参考实现与 C++ 加速实现（`raytrace.cpp`）语义一致，且有单测交叉验证（`tests/test_physics.py`）。

**发现的问题（按严重程度排序）：**

### 问题① 论文 Q1 表格与实际代码输出对不上（最重要）
论文 Table 1 与摘要写的是：Q1 年均功率 **35.43 MW**、单位面积 **0.564 kW/m²**、`η_trunc = 0.92926`、`η_sb = 0.92327`。
但代码实际跑出的 `output/validated/q1_initial.json`（4096 光线）是：**35.23 MW**、**0.5608 kW/m²**、`η_trunc ≈ 0.949`、`η_sb ≈ 0.917`。

关键证据：两版的**余弦效率完全一致**（论文 0.75647 vs 代码 0.7565），说明太阳几何/DNI/余弦没问题；差异**集中在阴影遮挡效率和截断效率**上。也就是说，论文那张表是用**旧版的阴影/截断模型**算的，和当前代码不同版。**论文必须用当前代码重新生成，否则"可复现"就名不副实。**

### 问题② `output_calc.txt` 和 `output/metrics.csv` 是旧算法残留
这两个文件里 `η_sb` 恒等于 0.95、`η_trunc` 恒等于 0.90（旧版"固定效率"算法），而且 q2 与 q3 的数据**完全相同**（旧版 q3 只是复制 q2）。它们与现在的代码（光线追踪）无关，但放在仓库里极易误导。建议清理或加"stale"标记。

### 问题③ 三处题目未给出、代码自行假设的物理量
- **塔身半径 = 2 m**（`Site.tower_radius`）——题目根本没给塔身粗细，但这会直接影响阴影/遮挡；
- **集热器只计"侧面命中"，端面命中一律不算**（`kind==1` 才计入）；
- **均匀亮度太阳圆盘**（视半径 4.65 mrad）。

这三条在 README/论文第 9 节有"声明"，算是诚实，但它们**实质性地影响 Q1/Q2/Q3 的绝对数值**，尤其塔身半径。评审角度这是"未论证的假设"而非硬 bug。

### 问题④ 论文没有给出 Q2/Q3 的最终数值
论文第 7 节把 Q2/Q3 结果"推给 `verification.json`"，但 `output/validated/` 目前只有 `q1.npz` 和 `q2_screening.json`，**Q2/Q3 的最终设计、`result2/3.xlsx`、`verification.json` 都还没生成**。作为竞赛论文，Q2/Q3 的"表 1/2/3"是必须填的，当前论文这个位置是空的。

### 问题⑤ 优化是启发式，无全局最优证明
这一点论文本身坦率声明了（第 8 节、结论节），不算"错"，但你要清楚：Q2/Q3 给出的只是"搜索预算内的较好可行解"，不是数学最优。

### 问题⑥（小）`problem/result2.xlsx`、`result3.xlsx` 其实是旧算法的结果
它们不是空模板，而是旧版 8m×8m、高度 6m、栅格间距 13.01m 的 1589 面镜布局（和 `verification.py: legacy_field()` 重建的"旧算法布局"一致）。程序把它们当只读输入并做 SHA-256 校验，但语义上它们是"旧答案"，容易和题目模板混淆。

---

## 二、题意拆解

**场景**：塔式光热电站，一个半径 350 m 的圆形镜场（东经 98.5°、北纬 39.4°、海拔 3000 m），中心建吸收塔，塔顶集热器中心高 80 m，集热器为**高 8 m、直径 7 m 的圆柱（外表受光式）**。镜场坐标：正东 = +x，正北 = +y，竖直向上 = +z。

**硬性约束（三问通用）**：
1. 镜面为矩形，宽 2–8 m、高 2–8 m 且**宽 ≥ 高**；
2. 安装高度 2–6 m，且镜面绕水平轴转动时**最低点离地 > 0**（等价 `z − h/2 > 0`）；
3. 相邻底座中心距 ≥ **较大镜面宽 + 5 m**；
4. 塔周 **100 m** 内不装镜；
5. 年均口径 = 每月 21 日的 9:00 / 10:30 / 12:00 / 13:30 / 15:00，共 **60 个等权时刻**。

**目标量**：年均输出热功率 `E = Σ_t DNI(t)·Σ_i A_i·η_i(t)`（等权平均 60 个时刻），以及单位面积功率 `E / Σ A_i`。

**三问要求**：
- **问题1**：塔在圆心 (0,0)，所有镜 6m×6m、高度 4m，位置由附件给定（1745 面），**只做计算**，填表（年均/逐月的光学、余弦、阴影遮挡、截断效率 + 功率 + 单位面积功率）。
- **问题2**：所有镜尺寸与安装高度**相同**，在**年均功率 ≥ 60 MW** 的约束下，**最大化单位面积功率**；要给出塔位置、镜尺寸、安装高度、镜数目、位置，输出 `result2.xlsx`。
- **问题3**：镜尺寸、安装高度**可以不同**，其余同问题2，输出 `result3.xlsx`。

数学上 Q2/Q3 可统一写为（论文 eq.1）：
$$\max_{\mathcal F}\ J=\frac{\overline P(\mathcal F)}{A(\mathcal F)},\qquad \overline P\ge 60000\ \text{kW}$$

---

## 三、公共光学模型（每一问的"计算内核"）

这是三问共用、也是这篇论文最有价值的部分。核心是**一次蒙特卡洛光线追踪同时算余弦、阴影遮挡、截断**，而不是事后把几个平均效率相乘。

**3.1 太阳与镜面姿态**（`model.py: solar_vector / Field.geometry`）
- 太阳方向直接写成分量（规避反三角分支）：
$$s=(-\cos\delta\sin\omega,\ \sin\delta\cos\varphi-\cos\delta\sin\varphi\cos\omega,\ \sin\delta\sin\varphi+\cos\delta\cos\varphi\cos\omega)$$
- 镜面法向取"太阳中心方向 s"与"镜心→集热器中心 t"的角平分线：`n = (s+t)/|s+t|`；
- 宽度方向 `u` 取水平且 ⟂ n，高度方向 `v = n×u`，从而**上下边始终水平**；
- 镜面点 `p = c + ξ·w·u + ζ·h·v`（ξ,ζ∈[−½,½]）。

**3.2 太阳圆盘与反射**（`optics.py: solar_rays / trace_numpy`）
- 太阳不是点光源，而是一个视半径 4.65 mrad 的**均匀亮度圆盘**；按"投影立体角均匀"采样方向 `s'`（`sinθ = sinα·√U`），并用扰乱 Sobol 同时采样镜面面积（2 维）与太阳圆盘（2 维），共 4 维积分；
- 反射方向 `r = 2(n·s')n − s'`；
- 权重 `q = max(0, n·s')/(s'·s)`，我推演过：这样采样后 `η_cos = C/K = (1/K)Σq` 恰好严格等于余弦效率（`E/DNI`），正确。

**3.3 遮挡与截断（同一批光线、能量账本）**
对每条采样光线做两次追踪：
1. **入射阴影**：沿 `p + λ·s'`（朝向太阳）检查是否撞到其它镜、塔身、集热器；
2. **反射遮挡**：沿 `p + λ·r` 检查是否先撞到塔身或其它镜；
3. **截断**：`r` 是否**首次命中圆柱侧面**（`kind==1`；命中端面 `kind==2` 不算，避免"先穿端面再穿侧面"被误计）。

设权重总和 C、去掉阴影+遮挡后 S、命中侧面 H、样本数 K：
$$\eta_{cos}=\frac C K,\quad \eta_{sb}=\frac S C,\quad \eta_{trunc}=\frac H S,\quad P_i=DNI\,A_i\,\eta_{at,i}\,\eta_{ref}\,\frac H K$$
阴影/遮挡按布尔并集统计，同一条光线不会重复扣损；截断与遮挡用同一批光线，保留相关性。

**3.4 空间筛选（`geometry.py: neighbor_lists`）**
因为入射、反射光线都朝上，超过最高镜面边缘就再也碰不到别的镜，据此推导每个镜的 2D 搜索半径，再用 KDTree + 光锥/外接球进一步裁剪；斜率误差使角界失效时自动回退全镜对。有单测对照全镜对验证不漏检。

---

## 四、问题1 的算法

**就是"纯计算"**：`evaluate(load_attachment(), rays=...)`，塔在原点、1745 面 6×6、高 4m。
流程：读附件 → 60 个时刻逐个算太阳/DNI/法线/基向量 → 每镜每时刻做光线追踪 → 汇总月均/年均效率与功率。
**没有优化，只有积分精度问题**（光线数收敛、独立随机化复核）。

注意点：`load_attachment()` 从 `min_row=2` 读 2 列坐标（我核对过附件确实是 1746 行 = 表头 + 1745 面镜，表头那行数值是 `[0,1]`，被正确跳过）。

---

## 五、问题2 的算法（统一镜型 + 多保真优化）

目标：`max P/A`，约束 `P ≥ 60 MW`。搜索变量（`optimize.py: decode`）：
塔址 `(x_T,y_T)`、统一宽 `w`、高 `h`、安装高度 `z`、间距余量、行距/阻挡因子、布局转角、两个相位、布局类别、分带环数。

**布局族**（`candidates()`）：
- **三角交错栅格**（staggered）：以 `s = w+5+ε` 为晶格，邻行平移半格，绕塔旋转；
- **分带径向交错**（rings）：一带内环上镜数相同、方位角等间隔、邻带错开半个角；
- **自适应径向环**（adaptive_rings）：径向间距随半径增大，`Δr ≥ max(s, λ·h·R·r/(2·(80−z)))`。

生成后仍逐对检查间距，不假设"理论上交错"。

**五个阶段**（论文 Algorithm 1 + `optimize.py: optimize_q2`）：
1. **初筛（乐观上界）**：`screen_task` 先算"无相互作用"上界，`面积×平均DNI×反射率` 不够 60 MW 的直接淘汰；再用 `min(1, P_coarse/60050)^8` 打分（`screen_merit`）保留多个参数盆地；
2. **全场少光线交互追踪**：对晋级候选做 60 时刻少光线完整追踪；
3. **容量恢复**：可行解不足时，`feasibility_search` 用 Sobol 扰动 + 精英差分做 6 代种群进化，目标先"提满场功率"、达标后再比单位面积；
4. **高精度局部搜索**：`refinement_rays` 提高光线数，精英差分扰动（论文 eq.190）+ 坐标搜索；
5. **删镜压缩面积**：`trim_field` 按"逐镜功率密度"从低到高删，每次删后**重新追踪**（删除只会减少遮挡源，剩余镜可见性单调不减，所以删镜是安全的面积压缩）。

> 关键洞察：删镜 + 重算遮挡，是"把多余功率换成更小面积"的合法手段，不能靠删前的静态排序直接宣布可行。

---

## 六、问题3 的算法（异构镜型）

**起点是问题2 的可行解**，不是重来（`optimize.py: optimize_q3`）：
1. **分组**：按相对塔的径向距离（1/3、2/3 分位）+ 东西侧，分成 6 组 `G_1..G_6`；
2. **分组试探**：每次只改一组的宽度/高度/安装高度（其它组不动），6 组轮一遍；
3. **容量恢复**：若面积减小导致功率跌破 60 MW，从"未占用且满足间距"的候选池按交互前收益补镜（`replenish`），或在仍有尺寸余量的高收益镜上回补高度；
4. **位置交换**：把低功率密度镜换到高收益候选位置；
5. **塔址小范围平移**：±4 m 图样搜索；
6. **逐镜微调**：对高收益单镜做小步长高度微调，把解落在 60 MW 边界附近以压缩面积。

**接受准则**：每次改动都要求"完整模型下 `P≥60 MW` **且** `J` 严格提高"才接受，杜绝以牺牲额定功率换表面上的面积收益。

> 局限（论文已坦承）：分组把数千个逐镜自由度压缩成 6 个区域变量，是"可解释的搜索方向"而非逐镜穷举；没有全局最优证明。

---

## 七、给你的建议（按优先级）

1. **用当前代码重新生成论文的 Q1 表格**（跑 `q1 --rays 4096` 后把 `q1_initial.json` 的月均值填进 Table 1），现在表格与代码不一致，这是最容易被评审抓的硬伤；
2. **补上 Q2/Q3 的最终结果表**（表 1/2/3 + `result2/3.xlsx`），目前论文里是空缺的；
3. **删除或隔离旧产物** `output_calc.txt`、`output/metrics.csv`，避免"固定 0.95/0.90 效率"的旧数据污染；
4. **把塔身半径 2m、只计侧面、均匀圆盘三条假设**在论文正文明示其对结果的敏感性（你已有 `sensitivity.json` 机制，但论文正文没展开）；
5. 如果追求"更像竞赛论文"，可在问题2里补一个**复杂度分析**（论文已有 `O(TNKM)`）和一个**收敛性/独立性说明**，这些代码里都已具备、只需在正文写清楚。

主要相关文件：`docs/paper.tex`（论文）、`heliostat/optics.py` + `raytrace.cpp`（光学内核）、`heliostat/optimize.py`（Q2/Q3 优化）、`heliostat/geometry.py`（相交与空间筛选）、`heliostat/verification.py`（独立复核）。

需要我帮你把论文 Q1 表格更新成代码实际值，或补写 Q2/Q3 的结果表框架吗？

好问题。我把代码里"算得慢、搜得糙、说不清"的地方都过了一遍，下面按**收益从大到小**给你一堆可优化的方向。每个方向我都会说清楚：**是什么、为什么有用、难不难、接在代码哪里**。最后给一个"如果只做三件事"的短名单。

---

## 第一类：让"评价器"变快、变准（收益最大）

现在所有搜索的瓶颈都在 `evaluate()`：每次都要对 60 个时刻 × 每镜几千条光线做蒙特卡洛。谁先把这一步提速，谁就赢。

### A1. 截断效率改用"卷积/查表"，干掉逐光线采样
**现状**：截断效率靠几千条 Sobol 光线数出来（`trace_numpy` 里的 `hit` 统计），占计算量大头，还带随机噪声。
**想法**：反射到集热器的光斑 = "镜子矩形 × 太阳圆盘"的卷积。把这个二维卷积做成解析近似（均匀圆盘用"圆与矩形交面积"积分，高斯太阳盘用误差函数 erf 的乘积公式），截断效率从"每镜几千次光线"降到"每镜几十个一维求积点甚至查表"，**零随机噪声、快两个数量级**。
**接在**：`optics.py` 里 `trace` 的 `cylinder_intersection` 那段，换成"把圆柱投影到垂直反射光线的平面上 → 计算光斑覆盖比例"。

### A2. 阴影/遮挡改用"多边形裁剪"，从蒙特卡洛变成精确几何
**现状**：阴影和遮挡也是逐光线布尔判定的（`rectangle_hits`）。
**关键观察**：平面镜反射的是平行光束。所以——**镜子 j 在镜子 i 上投下的阴影，就是 j 沿太阳方向投影到 i 所在平面上的一块多边形**；遮挡同理，沿反射方向投影。于是：
- 单对镜的阴影 = 两个矩形的**投影多边形相交面积**（Sutherland-Hodgman 裁剪即可）；
- 多面镜 = 投影多边形的**布尔并集**；
- 损失 = 并集面积 / 镜面面积，**精确、无噪声、几微秒算一对**。

太阳是圆盘带来的边缘"半影"可以通过把投影多边形按 `太阳半径 × 距离` 向外膨胀一圈来近似修正。
**接在**：`geometry.py`，新增 `polygon_shadow/blocking()`，替换 `trace` 里的 `rectangle_hits` 循环。

### A3. 副产品：整个评价变成确定性计算，验证体系大大简化
A1+A2 之后 `evaluate()` 不再有随机数，那么现在为蒙特卡洛噪声准备的整套东西——多组扰乱 Sobol、Student-t 下界、`certified()` 的置信区间——**都可以删掉**，`verification.json` 只剩约束检查和敏感性分析。搜索时也不用再担心"噪声把好方案误杀"。

### A4. 空间树每时刻重复建 → 只建一次
**现状**：`evaluate()` 的时间循环里，每个时刻都调 `neighbor_lists()`，里面重新 `cKDTree(field.centers[:,:2])`。但**镜心位置不随时间变**，树建 60 遍纯属浪费。
**想法**：把树建在时间循环外面，每个时刻只做"方向相关的锥形筛选"。改动小、白捡加速。
**接在**：`optics.py: evaluate` 与 `geometry.py: neighbor_lists`。

### A5. 粗筛不必算满 60 个时刻
**现状**：`screen_task` 用全部 60 个时刻 × 32 光线粗筛。相邻时刻高度相关，信息冗余。
**想法**：初筛只用 6~12 个代表时刻、把省下的光线加到每个时刻上（总成本不变）。排序质量几乎不降、单时刻精度更高。**接在** `screen_task` 的 `time_indices` 参数（代码已支持传子集）。

### A6. 单镜改动只重算"受影响者"（增量评价）
**现状**：Q3 的逐镜微调，改一面镜子就要全场重算（`individual refinement` 里每轮一次 `evaluate`）。
**想法**：动镜子 i 只影响两件事——i 自己的追踪，和"邻居列表里有 i"的那些镜子。只重算这些镜子，复杂度从 O(N) 降到 O(1+邻居数)。
**接在**：`optics.py` 加 `evaluate_delta(field, changed_index)`。实现略繁琐，但对 Q3 精修阶段是巨大提速。

---

## 第二类：搜索策略变聪明（收益中上）

### B1. 把现在的"多保真"正式写成 Successive Halving（连砍法）
**现状**：粗筛 → 中筛 → 精修是手写的固定三档。
**想法**：给所有候选按"少光线"打分 → 只留前一半 → 光线翻倍 → 再砍一半……直到剩下最终几个。预算分配更科学，能看更多候选。代码结构不用大改，就是把 `screening_rays/search_rays/refinement_rays` 变成循环里的变量。

### B2. 新增"向日葵螺旋"布局族（这是我个人最看好的一个）
**现状**：布局只有 3 族（三角栅格、径向环、自适应环），径向环的密度在"带边界"处会跳变。
**想法**：仿向日葵籽排列（Vogel/Fermat 螺线）：第 n 面镜的角度 = 黄金角 × n，半径 = c·n^p。**指数 p 一个参数就能连续控制"外圈比内圈稀疏多少"**（p > 0.5 时越往外越稀），间距 c 控制总体密度。只需 2 个旋钮，密度渐变、没有带间跳变，很可能比现在的环带布局更优。
**接在**：`optimize.py: candidates()` 加一个 `family == 'sunflower'` 分支。

### B3. 塔址外层循环 + "价值景观"预计算
**观察**：给定塔址和镜子尺寸，每面镜的"孤立收益"（余弦×大气×截断）只和它的位置有关。可以先把整个场地每一点的孤立收益**画成一张等高线地图**（2m 网格查表，一次算完反复用）。
**想法**：外层遍历塔的 y 坐标（东西对称所以 x≈0），内层对着这张地图做布局和选镜。既直观（能画出"哪里值得放镜子"），又大幅提速粗筛。
**接在**：`optics.py` 加 `value_landscape(tower, size)` 缓存。

### B4. 选镜子从"贪心前缀"升级为"带交换的局部搜索"
**现状**：`trim_field` 只按密度排序删镜子；Q2 没有"从候选池补镜子"的交换（Q3 才有 position_exchange）。
**想法**：给 Q2 也加"删一面低收益镜 ↔ 补一面候选池高收益镜"的交换搜索，配 `trim_field` 重算。删镜单调性不变，交换只要完整重算就仍然安全。

### B5. Q3 用"边际收益均衡"替代拍脑袋的 6 分组
**现状**：`q3_groups` 按半径三分位+东西侧硬分 6 组，每步整组一起改。
**想法**：算每面镜的"边际单位面积功率"（dP/dA_i）。经济学直觉：**最优时所有镜子边际收益应该相等**——收益低于平均的镜子该缩小，高于平均的该放大。于是 Q3 变成一轮轮"缩小的缩、放大的放、容量不够就补"的水位均衡迭代，比固定 6 组细得多，而且有理论故事可讲。
**接在**：替换 `optimize_q3` 的 `kind` 循环，`replenish/trim` 复用。

### B6. 布局问题等价于"带权重的圆盘装箱"，可用力导向/模拟退火
**关键转化**：均匀镜子时，"中心距 ≥ w+5" **完全等价于**"半径 (w+5)/2 的圆盘互不重叠"。于是布局 = 在环形区域（半径 100~350 挖掉禁区）里，把一堆圆盘放在 B2/B3 那张收益地图上，**总收益最大**。这是经典的带权圆盘装箱，模拟退火/力导向都能解，而且解不再被 3 个布局族锁死。
**接在**：`optimize.py` 新写一个 `packing_layout()` 作为第 4 种布局。

### B7. 多随机种子并行跑，取最好
**现状**：整个流水线单一种子 `202309`。
**想法**：搜索本来就支持 `--workers`，直接开 4 个种子各跑一遍取最优。零成本、白捡的改进，还能顺便估计"搜索的方差"。

### B8. 允许临时"越界"的模拟退火
**现状**：接受准则很硬——不达标或没进步就退回。容易卡在局部最优。
**想法**：搜索中段引入温度：允许**暂时**接受功率略低于 60 MW 或 J 略降的移动（逃离盆地），只在最终答案上严格保证可行。`optimize_q2/q3` 的接受判断处加个温度参数即可。

---

## 第三类：给论文加"含金量"（不改答案，但说服力大增）

### C1. 推导一个"全局上界"，把"没有最优性证明"变成"距离上界只差 X%"
这是论文最值钱的一句话。做法（用 B3 的收益地图）：
1. 把场地按孤立收益从高到低分层（等高线环带）；
2. 每个环带里**最多能塞多少面镜子**是确定的（环带面积 ÷ 每镜最小占地）；
3. 所以任何布局的总功率都 ≤ "各环带镜数上限 × 该带最高孤立收益"的和 → **一个谁都无法超越的上界**；
4. 拿自己答案和上界一比，差距就是 X%。

这样论文就能写"我们离全局最优不超过 X%"，比现在"不保证全局最优"强一百倍。
**接在**：`verification.py` 加 `upper_bound()`，输出进 `verification.json`。

### C2. 初筛用"一阶阴影模型"收紧上界
**现状**：`screen_task` 的做梦上界太松（完全不考虑挡光），过关的人太多、都要细算。
**想法**：加一个廉价的挡光估计（如相邻镜投影重叠面积的乘积近似，DELSOL 系软件的经典做法），把上界压紧，初筛就能多砍一批。

---

## 第四类：零碎但白捡的小改进

| 编号 | 改进 | 位置 |
|---|---|---|
| D1 | `trim_field` 每轮重算目前是必要的，但可在"离边界远"时一次删多批、只到最后验一次 | `optimize.py: trim_field` |
| D2 | C++ 里对邻居按距离排序、命中即提前退出（阴影判断已有早退，遮挡可同理） | `raytrace.cpp` |
| D3 | Q2 塔址搜索范围 y∈[−245,205]，若最优解贴着边界要警惕范围不够，放宽并确认 | `optimize.py: decode` |
| D4 | 布局生成里"每环镜数取整"可以变成小整数优化（对最优环数做 2~3 个备选） | `optimize.py: candidates` |

---

## 如果只做三件事，我建议

1. **A1+A2（解析截断 + 多边形阴影/遮挡）**——评价器又快又准又确定，所有后续搜索都跟着起飞，且 A3 让验证体系变简单。工作量最大但最值。
2. **B2+B6（向日葵螺旋 + 带权圆盘装箱）**——直接扩大布局搜索空间，是最可能真正把单位面积功率刷上去的一步。
3. **C1（全局上界）**——不改算法，但论文从"没办法证明"升级到"有界保证"，竞赛评审里这是大加分项。

再附一个性价比之王：**B7 多种子并行 + B1 连砍法**，几乎不动核心代码，当天就能做。

要不要我挑其中一个（比如 A2 的"多边形阴影/遮挡"或 B2 的"向日葵布局"）写一个可运行的实现草案出来？
